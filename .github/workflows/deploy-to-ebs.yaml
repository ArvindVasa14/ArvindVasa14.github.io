# name: Deploy Angular App to AWS Elastic Beanstalk

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Setup Node.js
#         uses: actions/setup-node@v2
#         with:
#           node-version: '18'

#       - name: Install dependencies
#         run: npm install

#       - name: Install express and path
#         run: npm install express path --save

#       - name: Build Angular app
#         run: npm run build --prod

#       - name: Zip application
#         run: zip -r angular-app.zip dist/*

#       - name: Deploy to Elastic Beanstalk
#         uses: einaregilsson/beanstalk-deploy@v14
#         with:
#           aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           region: 'ap-south-1'
#           application_name: 'arvind-angular-frontend'
#           environment_name: 'Arvind-angular-frontend-env'
#           version_label: "ver-${{ github.sha }}"
#           deployment_package: 'angular-app.zip'

